{
  "name": "@beacon/core",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first infra, network, and supply-chain scanning engine for Beacon v2",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
